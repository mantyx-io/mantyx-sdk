# session-chat

Interactive multi-turn REPL on top of `Client.CreateSession`. Type a message,
the assistant replies; press Ctrl+D to exit.

```bash
export MANTYX_API_KEY="mk_..."
export MANTYX_WORKSPACE_SLUG="acme-corp"

go run .
```
